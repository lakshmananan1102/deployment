 apiVersion: apps/v1
 kind: Deployment
 metadata:
   name: metric-service
 spec:
   replicas: 1
   selector:
     matchLabels:
       app: metric-service
   template:
     metadata:
       labels:
         app: metric-service
     spec:
       containers:
       - name: 1655-camera-07
         image: ghcr.io/sidharthram98/metric-deployment:latest
       imagePullSecrets:
       - name: {{SECRETS.NAME}}

